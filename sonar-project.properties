sonar.projectKey=A231871_yoedu-real-estate-web
sonar.organization=a231871

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=yoedu-real-estate-web
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=src

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# Exclude specific files from being analyzed
sonar.exclusions=node_modules/**, dist/**, build/**, public/**, **/*.test.tsx, **/*.spec.tsx, **/*.test.ts, **/*.spec.ts

# Include test files
sonar.tests=src
sonar.test.inclusions=**/*.test.tsx, **/*.spec.tsx, **/*.test.ts, **/*.spec.ts
